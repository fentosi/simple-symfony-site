{% if isSuccess != true %}
    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% else %}
    <p>Thanks for your message!</p>
{% endif %}
